apiVersion: apps/v1
kind: Deployment
metadata:
  name: logoutput-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: logoutput
  template:
    metadata:
      labels:
        app: logoutput
    spec:
      volumes:
        - name: shared-file
          emptyDir: {}
      containers:
        - name: logoutput-generator
          image: ipersids/logoutput-generator:ex1.10
          volumeMounts:
            - name: shared-file
              mountPath: /usr/src/app/files
          env:
            - name: PORT
              value: "3000"
            - name: "FILE_PATH"
              value: "/usr/src/app/files/log.txt"

        - name: logoutput-reader
          image: ipersids/logoutput-reader:ex1.10
          volumeMounts:
            - name: shared-file
              mountPath: /usr/src/app/files
          env:
            - name: PORT
              value: "3001"
            - name: "FILE_PATH"
              value: "/usr/src/app/files/log.txt"
          ports:
            - name: http
              containerPort: 3001
