apiVersion: apps/v1
kind: Deployment
metadata:
  name: todoapp-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todoapp
  template:
    metadata:
      labels:
        app: todoapp
    spec:
      volumes:
        - name: shared-file
          persistentVolumeClaim:
            claimName: file-claim

      containers:
        - name: todoapp
          image: ipersids/todoapp:ex2.2
          volumeMounts:
            - name: shared-file
              mountPath: /usr/src/app/files
          env:
            - name: PORT
              value: "3000"
            - name: "VOLUME_DIR"
              value: "/usr/src/app/files/"
            - name: TODO_BACKEND_URL
              value: "http://todobackend-svc:2348"
          ports:
            - name: http
              containerPort: 3000
