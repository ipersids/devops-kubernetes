apiVersion: v1
kind: Service
metadata:
  name: todoapp-svc
  namespace: project
spec:
  type: ClusterIP
  selector:
    app: todoapp # app as declared in the deployment
  ports:
    - name: http
      protocol: TCP
      port: 2346
      targetPort: http
