apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: file-claim
  namespace: project
spec:
  storageClassName: shared-kube-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
